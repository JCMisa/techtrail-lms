/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'img.clerk.com', 'i.sstatic.net', 'linda-hoang.com', 'utfs.io']
    }
};

export default nextConfig;
